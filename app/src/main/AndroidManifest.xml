<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk tools:overrideLibrary="androidx.health.connect.client" />

    <!-- Permissions for Connectivity and Maps -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Camera permission for barcode scanning -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Health Connect Permissions (Modern Android 14+) -->
    <uses-permission android:name="android.permission.health.READ_STEPS"/>
    <uses-permission android:name="android.permission.health.READ_HEART_RATE"/>

    <!-- Health Connect Permissions (Legacy Support for APK version - UPPERCASE required for some versions) -->
    <uses-permission android:name="androidx.health.permission.STEPS.READ" />
    <uses-permission android:name="androidx.health.permission.HEART_RATE.READ" />
    <!-- Adding Write as a dummy to improve discovery on some Samsung/Legacy systems -->
    <uses-permission android:name="androidx.health.permission.STEPS.WRITE" />

    <!-- Queries for external navigation apps (Android 11+) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <package android:name="com.google.android.apps.healthdata" />
        <package android:name="com.huawei.health" />
    </queries>

    <application
        android:name=".Thruxion"
        android:allowBackup="false"
        tools:replace="android:allowBackup,android:fullBackupContent,android:dataExtractionRules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.QhagoApp"
        android:hardwareAccelerated="true"
        android:extractNativeLibs="false"
        android:enableOnBackInvokedCallback="true"
        android:appCategory="social"
        tools:targetApi="35">

        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/Theme.QhagoApp.Login"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.register.RegisterActivity"
            android:theme="@style/Theme.QhagoApp.Login"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            tools:ignore="LockedOrientationActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.QhagoApp.NoActionBar"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop"
            android:exported="true">
            <!-- Huawei Auth Redirect (HTTPS) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="thruxion.com" android:pathPrefix="/huawei-auth" />
            </intent-filter>

            <!-- Huawei Auth Redirect (Custom Scheme Fallback for JS Redirect) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.thruxion.app" android:host="huawei-auth" />
            </intent-filter>

            <!-- Web3Auth / MetaMask Redirect -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.thruxion.app" android:host="auth" />
            </intent-filter>
        </activity>

        <!-- Health Connect Permissions Rationale -->
        <activity
            android:name=".ui.health.HealthPermissionsRationaleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
            <!-- Required for discovery on some Android 13 versions -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity>
    </application>

</manifest>
